import { useState } from "react";

const Work = () => {
    return (
        <div>
        {/* remove button to delete it */}
        {/* inputs to write the experience */}
        {/* add button to add a new one underneath */}
        </div>
    );
}

export default Work;
